# CLI utilities
click>=8.1,<9.0  # Allow future minor updates

# Testing tools
pytest>=7.2,<8.0  # Allow updates within the major version
pytest-cov>=4.0,<5.0  # Ensure compatibility but allow minor upgrades

# Code quality & static analysis
mypy>=0.991,<1.0  # Allow updates but keep within stable range
flake8>=6.0,<7.0  # Allow minor updates for improvements
